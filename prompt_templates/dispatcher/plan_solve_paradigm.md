ONLY when the user EXPLICITLY asks for a step-by-step plan (e.g. "先制定计划再执行", "make a plan first", "list the steps", "break this down into steps"), or when the task involves 4+ interdependent sub-tasks with clear dependencies (e.g. research across multiple sources → synthesise → write report, or refactor multiple files with dependency ordering).  Do NOT use this for "what did we talk about", "summarise our conversation", or any retrospective question about the current chat.